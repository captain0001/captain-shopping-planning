apiVersion: v1
kind: Service
metadata:
  name: shopping-svc
  namespace: shopping-planning
spec:
  selector:
    app: shopping-app
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30081   # 30080 は household-budget が使用中
  type: NodePort
